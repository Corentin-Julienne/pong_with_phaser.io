import { DisplayMsgTimestampPipe } from './display-msg-timestamp.pipe';

describe('DisplayMsgTimestampPipe', () => {
  it('create an instance', () => {
    const pipe = new DisplayMsgTimestampPipe();
    expect(pipe).toBeTruthy();
  });
});
